# Twilio Configuration
# Copy this file to .env and fill in your actual Twilio credentials
# DO NOT commit .env to version control

# Required for SMS and WhatsApp webhooks
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
# Optional: Use a different account SID specifically for SMS (falls back to TWILIO_ACCOUNT_SID if not set)
TWILIO_SMS_ACCOUNT_SID=your_twilio_sms_account_sid_here
TWILIO_SMS_AUTH_TOKEN=your_twilio_sms_auth_token_here
TWILIO_WHATSAPP_AUTH_TOKEN=your_twilio_whatsapp_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_WHATSAPP_PHONE_NUMBER=+14155238886

# Optional: Not currently used in the codebase
# TWILIO_USER_SID=your_twilio_user_sid_here

# Google Workspace Configuration
# For GOOGLE_SERVICE_ACCOUNT_KEY, you can either:
# 1. Provide the JSON content as a string (escape quotes properly)
# 2. Provide the path to the JSON file (for local development)
# Example: GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}
# Or: GOOGLE_SERVICE_ACCOUNT_KEY=./path/to/service-account-key.json
# NOTE: For AWS Lambda deployment, store this in Secrets Manager instead (see SECRETS_MANAGER_SETUP.md)
GOOGLE_SERVICE_ACCOUNT_KEY=your_google_service_account_key_json_here
# The email address in your Google Workspace that the service account will impersonate
GOOGLE_WORKSPACE_EMAIL=your-email@yourdomain.com

# Gmail OAuth2 Configuration (for Gmail Poller and OAuth flow)
# Required for Gmail polling and automated token management
# Get these from: Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
# The redirect URI must match what's configured in Google Cloud Console
# Example: https://your-api-gateway-url/gmail/oauth/callback
GMAIL_CLIENT_ID=your_gmail_oauth2_client_id_here
GMAIL_CLIENT_SECRET=your_gmail_oauth2_client_secret_here
GMAIL_REFRESH_TOKEN=your_gmail_oauth2_refresh_token_here
GMAIL_REDIRECT_URI=https://your-api-gateway-url/gmail/oauth/callback

# Gmail Poller Configuration (optional)
MAX_RESULTS=5
POLL_INTERVAL=5000
# SENDER_EMAIL: Comma-separated list of email addresses to filter by sender
# Example: SENDER_EMAIL=email1@example.com,email2@example.com,email3@example.com
# Leave empty to process all unread emails
SENDER_EMAIL=

